def count_words(text):
    """Counts word frequency in text and returns a dictionary."""
    counts = {}
    # TODO: Convert text to lowercase and split it into a list of words
    # TODO: Loop through the words and count their frequency in the 'counts' dict
    return counts